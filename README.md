# CLI Chatbot (Groq)

A command-line chatbot built with Python and Groq's API (llama-3.3-70b-versatile).
Keeps conversation history across turns. Type `stop` to exit.

## Setup

1. pip install groq
2. Copy `api_key.example.py` to `api_key.py` and add your Groq API key
3. python main.py
